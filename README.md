# Install and run
```sh
cd backend
pnpm install
pnpm run start:dev
```

```sh
cd frontend
pnpm install
pnpm run dev
```

>> in reactnative, `import { RTCView, mediaDevices } from 'react-native-webrtc'`
